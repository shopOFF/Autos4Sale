@model IEnumerable<Autos4Sale.Web.ViewModels.CarOfferViewModel>

@{
    ViewBag.Title = "AllCars";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    @{
        if (Model.Count() < 1)
        {
            <h1>No Offers To Display</h1>
        }
        else
        {
            <h2>All Car Offers</h2>
            <hr />
            foreach (var offer in Model)
            {
                <a href="@Url.Action("Details", "Offers")" class="details-link">
                    <div class="well">
                        <div class="img-container">
                            @{
                                var item = offer.Image.FirstOrDefault();
                            }
                            <tr>
                                <td><img src="~/Images/@item.ImageUrl" class="img-thumbnail" /></td>
                            </tr>
                        </div>
                        <div class="text-container">
                            <h4><strong>@offer.Brand @offer.Model</strong></h4>
                            <h4 class="price"><strong>€@offer.Price</strong></h4>
                            <h5>Year Manufactured: @offer.YearManufacured</h5>
                            <h5>Date Added: @offer.ModifiedOn</h5>
                        </div>
                    </div>
                </a>
            }
        }
    }
</div>
